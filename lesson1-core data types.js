// Core Data Types

// Number
var numbers = 1;
var numbers = 1.0;

// Strings
var str = 'value is in single quotes';
var str2 = "value is in single quotes";
var stringEscapingQuotes = 'with \'quoted\' text'; // with 'quoted' text
var stringEscapingBackslash = 'with a backslash \\ like that'; // with a backslash \ like that

// Arrays
var emptyArray = [ ];
var arrayOfNumber = [1, 2, 3];
var arrayOfString = ['one', 'two', 'three'];

//NOTE: We did not talk about "Object"
